# Протоколи керування лампою

Документ описує протоколи, які реалізовані у `C:\Users\temaune\Desktop\library.py`.

У файлі присутні два різні класи пристроїв:

| Клас | Транспорт | Порт / канал | Протокол |
|---|---|---|---|
| `SmartBulb` | TCP/IP | TCP `5577` | MagicHome-подібний протокол |
| `BluetoothSmartBulb` | Bluetooth Low Energy | GATT writable characteristic | Набір сумісних BLE-форматів |

Важливо: BLE-клас не визначає протокол конкретної лампи. Він послідовно надсилає кілька різних форматів, розраховуючи, що лампа розпізнає один із них.

## 1. Спільні поняття

| Позначення | Значення |
|---|---|
| `r`, `g`, `b` | Червоний, зелений і синій канал, `0..255` |
| `ww` | Warm White, теплий білий, `0..255` |
| `cw` | Cool White, холодний білий, `0..255` |
| `level` | Яскравість у відсотках, `0..100` |
| `checksum` | Контрольна сума, якщо явно вказана форматом |
| little-endian | Молодший байт багатобайтного числа перед старшим; у цьому коді багатобайтних полів немає |

Перетворення відсотків у байт:

```text
channel = round(level * 2.55)
```

Для RGB значення просто обмежуються діапазоном `0..255`.

---

# TCP MagicHome

## 2. З'єднання і передача

`SmartBulb` підключається до IPv4-адреси лампи через TCP-порт `5577`.

| Етап | Реалізація |
|---|---|
| Створення сокета | `AF_INET`, `SOCK_STREAM` |
| Опція затримки | `TCP_NODELAY = 1` |
| Таймаут | Типово `3.0` секунди для клієнта, `0.5` секунди для discovery |
| Передача | `sendall(data)` |
| Відповідь | Читається лише коли `wait_reply=True` або `read_reply=True` |
| Розмір читання відповіді | До 64 байт |
| Повторні спроби | `max_retries + 1`, типово 3 спроби |
| Затримка retry | Типово 50 мс |

Клас не додає довжину кадру, стартовий маркер або escape-послідовності. Надсилається рівно той масив байтів, який переданий у `_send()`.

## 3. Контрольна сума TCP

Функція:

```python
checksum = sum(data) & 0xFF
```

Контрольна сума є простою сумою всіх попередніх байтів за модулем `256`.

| Властивість | Значення |
|---|---|
| Розмір | 1 байт |
| Входять байти checksum | Ні |
| Алгоритм | Сума байтів modulo `256` |
| Порядок | Checksum додається в кінець кадру |

Приклад для RGB-червоного:

```text
Дані:      31 FF 00 00 00 0F
Сума:      31 + FF + 00 + 00 + 00 + 0F = 13F
Checksum:  3F
Кадр:      31 FF 00 00 00 0F 3F
```

## 4. Кадр увімкнення і вимкнення

Ці два кадри є короткими командами з уже готовою checksum.

| Дія | Байти | Розбір |
|---|---|---|
| Увімкнути | `71 23 0F A3` | `71` команда; `23` код ON; `0F` параметр/маркер; `A3` checksum/завершення |
| Вимкнути | `71 24 0F A4` | `71` команда; `24` код OFF; `0F` параметр/маркер; `A4` checksum/завершення |

Для цих кадрів код не обчислює checksum під час виконання, а використовує константи.

## 5. Встановлення RGB

Методи: `set_rgb()`, `frame_rgb()`.

Формат:

| Індекс | Поле | Значення |
|---:|---|---|
| `0` | Команда | `0x31` |
| `1` | Червоний | `r`, `0..255` |
| `2` | Зелений | `g`, `0..255` |
| `3` | Синій | `b`, `0..255` |
| `4` | Додатковий канал | `0x00` |
| `5` | Режим/маркер | `0x0F` |
| `6` | Checksum | `sum(bytes[0:6]) & 0xFF` |

Шаблон:

```text
31 rr gg bb 00 0F ss
```

Приклад `set_rgb(255, 0, 128)`:

```text
31 FF 00 80 00 0F 8F
```

## 6. Яскравість

`set_brightness(level)` не надсилає окремий brightness-байт. Він масштабує збережений базовий RGB-колір і повторно надсилає RGB-кадр.

```text
scaled_r = int(base_r * level / 100)
scaled_g = int(base_g * level / 100)
scaled_b = int(base_b * level / 100)
```

Отже, якщо базовий колір `(255, 128, 64)`, а рівень `50`, кадр міститиме `(127, 64, 32)` через використання `int()`, а не округлення.

`set_white_brightness()` спочатку встановлює базовий колір `(255, 255, 255)`, потім застосовує цей алгоритм.

## 7. Теплий і холодний білий через TCP

### Warm White

Метод `set_warm_white(level)` формує такий самий 7-байтний кадр:

| Індекс | Поле | Значення |
|---:|---|---|
| `0` | Команда | `0x31` |
| `1` | R | `0x00` |
| `2` | G | `0x00` |
| `3` | B | `0x00` |
| `4` | WW | `round(level * 2.55)` |
| `5` | Режим/маркер | `0x0F` |
| `6` | Checksum | Сума індексів `0..5` modulo `256` |

Шаблон:

```text
31 00 00 00 ww 0F ss
```

### Cool White

`set_cool_white(level)` спочатку викликає `get_status()` і перевіряє, чи є окремий канал CW. Якщо підтримка визначена, кадр має формат:

```text
31 00 00 00 00 cw 0F ss
```

Якщо підтримка не визначена, метод викликає `set_warm_white(level)`.

## 8. Запит статусу TCP

Запит:

```text
81 8A 8B 96
```

Метод `query_status()` надсилає його з `wait_reply=True` і читає до 64 байт.

Очікувана відповідь має щонайменше 14 байт і повинна починатися з `0x81`.

### Розбір відповіді статусу

| Індекс | Поле Python | Призначення в коді |
|---:|---|---|
| `0` | тип | Має бути `0x81` |
| `1` | `model` | Код моделі |
| `2` | `power_code` | `0x23` означає увімкнено |
| `3` | `preset` | Поточний preset |
| `4` | `mode` | Режим роботи |
| `5` | `speed` | Швидкість ефекту |
| `6` | `r` | Червоний канал |
| `7` | `g` | Зелений канал |
| `8` | `b` | Синій канал |
| `9` | `ww` | Теплий білий |
| `10` | `version` | Версія/додаткове поле |
| `11` | `cw` | Холодний білий |
| `12` | `color_mode` | Режим кольору |
| `13` | `checksum` | Контрольна сума відповіді, але код її не перевіряє |

Обчислена властивість:

```text
brightness = round(max(r, g, b, ww, cw) / 255 * 100)
```

### Важливе обмеження статусу

`parse_status()` лише читає поле `data[13]` у результат. Він не перевіряє, що checksum відповіді правильна. Також код не перевіряє точну довжину або формат додаткових байтів після індексу `13`.

## 9. Пакетні режими TCP

| Метод | Поведінка |
|---|---|
| `send_raw(data)` | Надсилає довільний кадр |
| `send_batch(commands)` | Надсилає кадри по одному через `_send()` |
| `send_burst(commands)` | Надсилає кадри послідовними `sendall()` без паузи |
| `send_burst_joined(commands)` | Склеює всі кадри в один буфер і робить один `sendall()` |
| `send_pipeline(commands, delay_ms)` | Надсилає кадри з фіксованою паузою |
| `send_repeat(command, times)` | Повторює один кадр задану кількість разів |

Ці методи не змінюють формат кадрів і не додають власного заголовка.

## 10. Виявлення MagicHome у мережі

### UDP broadcast

Надсилається ASCII-повідомлення:

```text
HF-A11ASSISTHREAD
```

на `255.255.255.255:48899`.

У відповідях код шукає регулярним виразом IPv4-адреси. Формат відповіді повністю не розбирається.

### TCP scan

Для кожної IP-адреси з діапазону надсилається:

```text
81 8A 8B 96
```

IP вважається знайденою, якщо лампа повернула хоча б один байт відповіді.

---

# Bluetooth LE

## 11. Підключення і пошук characteristic

`BluetoothSmartBulb` використовує `BleakClient`.

Алгоритм підключення:

1. Підключення до MAC-адреси або UUID.
2. Перебір усіх services і characteristics.
3. Пріоритетний пошук characteristic зі списку `CHAR_UUIDS`.
4. Якщо збігів немає, вибирається перша не-стандартна writable characteristic.
5. Для всіх characteristics із `notify` виконується спроба `start_notify()`.

Пріоритетні UUID:

| UUID | Коментар у коді |
|---|---|
| `0000ffe9-0000-1000-8000-00805f9b34fb` | MagicHome |
| `0000ff01-0000-1000-8000-00805f9b34fb` | ELK-BLEDOM / Triones / IOT |
| `0000ff11-0000-1000-8000-00805f9b34fb` | Частина дешевих ламп |
| `0000ffd1-0000-1000-8000-00805f9b34fb` | Деякі MagicBlue |

`SERVICE_UUID` оголошений як `0000ffe5-...`, але в коді фактично не використовується як фільтр.

## 12. BLE передача

Метод `async_send(data)`:

1. За потреби підключається.
2. Вимагає знайдену `_char_uuid`.
3. Виконує `write_gatt_char(_char_uuid, data, response=False)`.
4. Якщо запис не вдався, повторює з `response=True`.
5. Якщо існує інша writable characteristic з UUID, що містить `ff11`, дублює туди пакет.

Це евристика, а не підтверджена схема конкретної лампи. Надсилання на першу writable characteristic може бути небезпечним для невідомого пристрою.

## 13. BLE: увімкнення

`async_turn_on()` надсилає **шість різних кадрів послідовно**.

| Варіант | Байти | Призначення |
|---|---|---|
| Zengge / MagicHome | `71 23 0F A3` | Класичний кадр ON |
| Surplife | `41 01 01 00 00 00 00 00 00 00 00 00 00 00 00 01` | Заголовок `41 01 01`, 12 нульових байтів, checksum |
| Triones / HappyLighting | `CC 23 33` | Короткий кадр ON |
| LED-BLE / Lotus Lantern | `7E 00 04 01 00 00 00 00 EF` | Заголовок, тип `04`, стан `01`, завершення `EF` |
| MagicBlue V2 | `BB 25 05 44` | Короткий кадр ON |
| Broadlink | `A1 01 01 00 00 00 00 00 00 00 00 00 00 00 00 01` | Заголовок `A1 01 01`, нулі, checksum |

Для кадрів Surplife і Broadlink останній байт дорівнює сумі всіх попередніх байтів modulo `256`.

## 14. BLE: вимкнення

| Варіант | Байти | Призначення |
|---|---|---|
| Zengge / MagicHome | `71 24 0F A4` | Класичний кадр OFF |
| Surplife | `41 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00` | Код стану `00`, checksum `00` |
| Triones | `CC 24 33` | Короткий кадр OFF |
| LED-BLE | `7E 00 04 00 00 00 00 00 EF` | Код стану `00` |
| MagicBlue V2 | `BB 26 05 45` | Короткий кадр OFF |
| Broadlink | `A1 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00` | Код стану `00`, checksum `00` |

## 15. BLE: RGB

Метод `async_set_rgb(r, g, b)` надсилає шість форматів.

### Варіант Zengge / MagicHome

| Індекс | Байти | Значення |
|---:|---|---|
| `0` | `0x31` | Команда RGB |
| `1` | `r` | Червоний |
| `2` | `g` | Зелений |
| `3` | `b` | Синій |
| `4` | `0x00` | Резерв / WW |
| `5` | `0x0F` | Маркер режиму |
| `6` | checksum | Сума індексів `0..5` modulo `256` |

Шаблон:

```text
31 rr gg bb 00 0F ss
```

### Варіант Surplife

| Індекс | Значення |
|---:|---|
| `0` | `0x41` |
| `1` | `0x05` |
| `2` | `0x03` |
| `3` | `0x00` |
| `4` | `r` |
| `5` | `g` |
| `6` | `b` |
| `7..14` | `0x00` |
| `15` | Сума індексів `0..14` modulo `256` |

### Варіант Triones

```text
56 rr gg bb 00 F0 AA
```

| Індекс | Значення |
|---:|---|
| `0` | `0x56` |
| `1..3` | `r g b` |
| `4` | `0x00` |
| `5` | `0xF0` |
| `6` | `0xAA` |

У коді checksum для цього формату не обчислюється.

### Варіант LED-BLE / Lotus Lantern

```text
7E 00 05 03 rr gg bb 00 EF
```

| Індекс | Значення |
|---:|---|
| `0..2` | `7E 00 05` |
| `3` | `0x03`, RGB-режим |
| `4..6` | `r g b` |
| `7` | `0x00` |
| `8` | `0xEF`, завершення |

### Варіант MagicBlue V2

```text
BB 28 03 rr gg bb ss
```

| Індекс | Значення |
|---:|---|
| `0` | `0xBB` |
| `1` | `0x28` |
| `2` | `0x03` |
| `3..5` | `r g b` |
| `6` | Сума індексів `0..5` modulo `256` |

### Варіант Broadlink

| Індекс | Значення |
|---:|---|
| `0` | `0xA1` |
| `1` | `0x05` |
| `2` | `0x03` |
| `3` | `0x00` |
| `4..6` | `r g b` |
| `7..14` | `0x00` |
| `15` | Сума індексів `0..14` modulo `256` |

## 16. BLE: теплий білий

`async_set_warm_white(level)` надсилає чотири формати.

| Варіант | Шаблон | Checksum |
|---|---|---|
| Zengge / MagicHome | `31 00 00 00 ww 0F ss` | Сума перших 6 байт |
| Surplife | `41 05 01 00 00 00 00 ww 00 00 00 00 00 00 00 ss` | Сума перших 15 байт |
| Triones | `56 00 00 00 ww 0F AA` | У коді не обчислюється |
| LED-BLE | `7E 00 01 ww 00 00 00 00 EF` | Фіксоване завершення `EF` |

Для Surplife `ww` стоїть на індексі `7`; для Zengge на індексі `4`; для Triones на індексі `4`; для LED-BLE на індексі `3`.

## 17. BLE: яскравість

`async_set_brightness(level)` працює не як окремий brightness-командний байт. Він масштабує поточний RGB-колір:

```text
br = int(base_r * level / 100)
bg = int(base_g * level / 100)
bb = int(base_b * level / 100)
```

Після цього надсилаються три формати:

| Варіант | Формат |
|---|---|
| Zengge / MagicHome | RGB-кадр `31 br bg bb 00 0F ss` |
| Surplife | RGB-кадр `41 05 03 00 br bg bb 00... ss` |
| Triones | `56 br bg bb 00 F0 AA` |

На відміну від `async_set_rgb`, для brightness код не надсилає LED-BLE, MagicBlue V2 або Broadlink кадри.

## 18. BLE синхронний API

Синхронні методи запускають окремий asyncio event loop у daemon-потоці.

| Метод | Асинхронний метод |
|---|---|
| `connect_sync()` | `connect()` |
| `turn_on()` | `async_turn_on()` |
| `turn_off()` | `async_turn_off()` |
| `set_rgb(r,g,b)` | `async_set_rgb(r,g,b)` |
| `set_warm_white(level)` | `async_set_warm_white(level)` |
| `set_cool_white(level)` | Викликає `set_warm_white(level)` |
| `set_brightness(level)` | `async_set_brightness(level)` |

## 19. Що в коді не є протоколом лампи

| Частина | Що вона робить |
|---|---|
| `_base_color` | Локальний стан Python для розрахунку brightness |
| `_current_color` | Локальний останній RGB-стан |
| `debug` | Друк hex-кадрів, не змінює протокол |
| `max_retries` | TCP retry-поведінка, не поле кадру |
| `_run_sync()` | Міст між синхронним і async API |
| список `CHAR_UUIDS` | Евристика вибору BLE characteristic, не доказ протоколу |
| `start_notify()` | Спроба ввімкнути notifications; callback у коді нічого не розбирає |

## 20. Важливі ризики та неточності

1. `BluetoothSmartBulb.async_turn_on()`, `async_turn_off()` і `async_set_rgb()` надсилають кілька несумісних форматів підряд. Це зручно для експерименту, але не доводить, який формат підтримує пристрій.
2. BLE-клас вибирає першу writable characteristic, якщо UUID не знайдений у списку. Для іншого пристрою це може бути неправильний канал.
3. У `async_send()` альтернативний `ff11` може отримати дубльований пакет.
4. У BLE-методах немає перевірки відповіді пристрою після успішного `write_gatt_char()`.
5. Для частини BLE-форматів останній байт є маркером завершення, а не обов'язково checksum.
6. `set_cool_white()` у BLE-класі фактично викликає warm-white і не реалізує окремий CW-протокол.
7. `parse_status()` не перевіряє checksum відповіді.
8. Методи `send_burst`, `send_pipeline` та `send_repeat` не виконують retry і не перевіряють повний стан лампи.
9. `send_burst_joined()` склеює кадри без роздільника. Це коректно лише якщо TCP-приймач лампи вміє розбирати потік кадрів.
10. Коментарі про назви протоколів (`Surplife`, `Triones`, `Broadlink` тощо) походять із припущення автора коду. Для точного визначення моделі потрібен захват BLE/TCP-трафіку або документація пристрою.

## 21. Коротка карта команд

| Дія | TCP MagicHome | BLE Zengge | Інші BLE формати |
|---|---|---|---|
| Увімкнути | `71 23 0F A3` | `71 23 0F A3` | Surplife, Triones, LED-BLE, MagicBlue V2, Broadlink |
| Вимкнути | `71 24 0F A4` | `71 24 0F A4` | Surplife, Triones, LED-BLE, MagicBlue V2, Broadlink |
| RGB | `31 rr gg bb 00 0F ss` | Те саме | 5 альтернативних форматів |
| Warm White | `31 00 00 00 ww 0F ss` | Те саме | Surplife, Triones, LED-BLE |
| Cool White | Окремий/резервний канал | Не реалізовано окремо | Не реалізовано окремо |
| Brightness | Масштабування RGB | Масштабування RGB | Surplife та Triones додатково |
| Статус | `81 8A 8B 96` | Не реалізовано | Не реалізовано |

## 22. Висновок

Найкраще підтверджений протокол у `library.py` — TCP MagicHome:

- порт `5577`;
- RGB-кадр починається з `0x31`;
- останній байт RGB/WW-кадру — сума попередніх байтів modulo `256`;
- ON/OFF мають короткі кадри `71 23 0F A3` і `71 24 0F A4`;
- статус запитується кадром `81 8A 8B 96`.

BLE-реалізація є compatibility-layer для кількох популярних форматів. Для конкретної лампи потрібно зафіксувати фактичний service/characteristic, один прийнятий формат кадру і підтвердити його поведінку через відповідь або зміну стану.
